# Night Code 1395

중학교 3학년 「별과 우주」 단원 **수업 두 차시(각 45분)를 처음부터 끝까지 진행하는 학습 도구**.
단순 측정 도구가 아니라, 교사가 화면을 순서대로 넘기면 그것이 곧 수업이 되도록 만들었다.

국보 **천상열차분야지도**를 소재로, 학생이 별 자국의 크기를 직접 재어
"밝은 별일수록 크게 새겼다"는 규칙을 스스로 발견한다.

## 10단계 · 두 차시

### 1차시 — 발견하고 가설 세우기 (45분)

| 단계 | 화면 | 하는 일 | 시간 |
|---|---|---|---|
| 1 | 인트로 | "1395년, 조선…" 문구가 천천히 떠오른 뒤, 다음 장면에서 **수업 코드로 함께 붙는다** (2화면) | 5분 |
| 2 | **고천문학** | 옛 기록과 유물로 옛 하늘을 연구하는 학문 / 무엇을 자료로 쓰는가 / 고천문학자 영상 (3화면) | 6분 |
| 3 | 지도 소개 | 이름 풀이 / 대동강에 빠진 별지도 / 별이 알려 준 고구려 하늘 / 숫자로 보기 / 돌이 겪은 일 / 만원권 뒷면 — 이야기 카드 6장 | 10분 |
| 4 | 확대해 들어가기 | 전체 지도에서 삼수 영역으로 줌인 애니메이션 | 4분 |
| 5 | **두 지도와 가설** | 채색본 ↔ 각석본, 세 층(밤하늘→종이→돌) 밀어보기 → **가설 세우기(예상·이유·확인 방법)** → **확인할 가설 고르기** → **밝기 순서 맞히기** → 영상 → **1차시 마무리** (7화면) | 20분 |

### 2차시 — 직접 재서 확인하기 (45분)

| 단계 | 화면 | 하는 일 | 시간 |
|---|---|---|---|
| 6 | **월하정인** | 지난 시간 되짚기 → 신윤복 그림 속 달의 이상한 점 → 추리 → "이것이 고천문학이다" | 8분 |
| 7 | 겉보기 등급 | 밝기 차이 → **히파르코스** → "숫자가 작을수록 밝다" → 0·마이너스 등급 → **손잡이로 등급 바꿔 보기** → *(심화: 포그슨의 100배 규칙)* → **우리 곁의 천체 등급표 + 지난 시간 순위 맞춰 보기** (7화면) | 12분 |
| 8 | 측정하기 | 핀 10개, 확대 측정 뷰, 두 손잡이로 지름 재기, **1차·2차 두 번씩**, 교사 화면에는 별마다 몇 명이 쟀는지 | 15분 |
| 9 | 결과 확인 | 표 → 점 그래프(애니메이션) → "이런 걸 산점도라고 한다" → 경향선, **[내 결과 ↔ 우리 반 전체] 전환** | 7분 |
| 10 | 결론 | 내 결론 쓰기 → 정리 문구 → **친구들의 결론이 양옆에 흐른다** → 두 지도의 의미 → **가설 되돌아보기** | 3분 |

교사는 단계 표시줄로 어느 단계로든 즉시 건너뛸 수 있고,
설명 중심 화면(2·3·5·6·7·9)에는 작은 [건너뛰기 ▸] 버튼이 있다.
표시줄의 5단계와 6단계 사이에는 **2차시** 경계선이 그어져 있다.

**심화 화면(7-6, 포그슨)은 [다음]으로 넘길 때 건너뛴다.**
성취기준 [9과15-01] 해설이 수식 도입을 빼고 겉보기·절대등급의 개념 이해를
중심에 두기 때문이다. 필요한 학급만 7-5 화면의 [심화] 단추로 들어간다.

## 특징

- **Cloudflare Worker 하나로 끝난다** — 외부 데이터베이스·로그인·CDN 없음.
- 학생 기기가 처음 받는 코드는 **약 148KB**. 교사만 쓰는 캘리브레이션과 데모봇은
  필요할 때만 따로 읽어, 학생 태블릿에서는 내려받지도 해석하지도 않는다.
- **실명·학번·반은 서버로 가지 않는다.** 진행 상태와 원본 측정값은 localStorage에 남고,
  학급 기능에서는 학생이 스스로 정한 **닉네임**과 함께 보려고 올리는 것만 오간다.
- 바닐라 HTML/CSS/JS. 그래프·도식·삽화·밤하늘까지 전부 **코드로 직접 그린다.**
- 터치 우선 — 모든 버튼과 손잡이가 **44px 이상**, 화면 확대 방해 없음, 글자 크기 3단계.
- 휴대폰·태블릿·컴퓨터를 모두 확인했다. 좁은 화면에서는 기록표가 **카드로 접히고**,
  눕힌 화면에서는 측정 창이 **두 단**이 되어 [기록하기] 가 늘 보인다.
- 새로고침해도 이어서 진행된다. 도움말 안의 [전체 초기화]로 리셋.
- 정보를 색에만 기대지 않는다(완료 핀은 ✓, 집계는 숫자 병기).

## 단계 5의 세 층 — 밤하늘 → 종이 → 돌

손잡이를 오른쪽으로 밀면 새 층이 **왼쪽에서부터** 덮어 온다.
하늘에 있던 별이 종이에 옮겨지고, 다시 돌에 새겨지기까지의 순서다.

1. **실제 밤하늘** — 맨 아래. 사진이 아니라, 각석본에서 찾아낸 자국 542개를
   지름에서 등급으로 되돌려(지름 = 31.3 − 4.01 × 등급) 밝기로 그린 것
2. **종이(채색본)** — 별의 위치는 정확하지만 크기가 모두 같다
3. **돌(각석본)** — 맨 위. 별마다 크기가 다르다

맨 아래 층 덕분에 "돌에 새긴 크기를 밝기로 읽으면 이런 하늘"이 눈으로 보인다.
자료는 `assets/js/skydata.js` 에 미리 구워 두어 태블릿에서 이미지 분석을 하지 않는다.

밝은 별은 가운데가 빈 고리라 침식하면 덩어리가 조각나 중심이 어긋난다. 그래서 광선
바깥 끝점들의 무게중심으로 중심을 세 번 다시 잡고, 지름은 36방향 광선 길이의 **75번째
백분위수 × 2** 로 삼는다(학생에게 안내하는 "바깥 테두리의 바깥쪽 끝" 기준과 같다).
이렇게 재면 확정된 별 10개에서 등급과의 상관이 **−0.734** 로, 교사가 손으로 잰 값과 같다.
별자리 선이 붙어 있으면 자동 검출이 놓치므로, 좌표를 확정한 별 10개는 그 좌표와 등급으로
덮어쓴다 — 삼수의 별이 빠지지 않도록.

## 학급으로 같이 하기 (단계 1에서 참여)

수업 첫 화면에서 다 같이 붙는다. **교사가 화면을 넘기면 학생 화면도 같이 넘어간다.**

1. 인트로 문구가 끝나고 [시작하기] 를 누르면 참여 화면이 나온다.
   교사가 **[수업 열기]** → 6자리 수업 코드가 뜬다. 이 코드를 불러 준다
2. 학생은 자기 기기에서 **[코드로 참여하기]** → 코드와 **닉네임**을 넣는다
3. 교사 화면에 참여자 명단이 뜬다. 접속이 끊긴 학생은 흐리게 보인다
4. 이후 교사가 단계를 넘길 때마다 학생 화면이 따라온다
5. 단계 5-3에서 예상을 고르면 모든 화면의 막대가 실시간으로 움직이고,
   학생이 적은 **확인 방법**이 교사 화면에 닉네임과 함께 모인다
6. 단계 5-5에서 학생이 낸 밝기 순서가 교사 화면에 표로 모인다
7. 단계 6(2차시 첫 화면)에서 지난 시간의 예상과 확인 방법을 다시 꺼내 보여 준다
8. 단계 8에서는 별마다 몇 명이 쟀는지가 표에 함께 나온다 — 1차만 재도 센다
9. 단계 9에서 [우리 반 전체]를 누르면 반 전체의 산점도와 평균 경향선이 나온다
10. 단계 10에서 결론을 쓰면 친구들의 결론이 포스트잇처럼 양옆에 흐른다

- **학생은 따라가기를 끌 수 있다.** 상단 띠의 [따라가는 중]을 누르면 멈추고,
  다시 누르면 곧바로 교사 화면으로 돌아간다. 단계 8처럼 각자 속도로 재야 할 때 쓴다
- **별을 재는 중에는 화면을 빼앗지 않는다.** 측정 창을 닫을 때 밀린 단계로 따라간다
- **한 기기 한 표.** 다시 고르면 바뀌고 쌓이지 않는다
- 교사 화면의 명단에는 **누가 아직 안 골랐는지**만 보인다. 무엇을 골랐는지는 보이지 않는다
- 서버로 가는 것은 기기마다 만든 **무작위 토큰**과, 수업 중에 함께 보려고 올리는 것뿐이다 —
  학생이 정한 **닉네임**, 고른 **예상**, 적은 **이유와 확인 방법**, **밝기 순서 답**,
  잰 **자국의 크기**(등급·지름 짝), **결론 한 문장**.
  실명·학번·반은 보내지 않는다. 닉네임 12자·이유와 방법 160자·결론 300자로 자르고 태그 기호를 지운다
- **8일**이 지나면 그 수업의 기록은 자동으로 지워진다.
  두 차시를 한 주 간격으로 해도 같은 코드로 이어붙을 수 있게 잡은 값이다
- **연결이 안 돼도 앱은 그대로 동작한다.** 참여 칸만 조용히 접힌다

구현은 Cloudflare **Durable Object** 하나다(수업 하나 = 객체 하나).
그 안에 상태와 WebSocket 이 함께 있어 외부 데이터베이스가 필요 없고,
SQLite 저장소를 쓰므로 무료 요금제에서도 된다.

## 두 차시를 잇기 — 지난 수업 코드로 다시 열기

1차시에 모인 반 전체 자료(예상 집계·확인 방법·밝기 순서 답)는 **수업 코드에 붙어 있다.**
2차시에 새 코드를 열면 그 자료가 따라오지 않으므로, 참여 화면에서
**[지난 수업 코드로 이어서 열기]** 를 눌러 1차시에 쓴 코드로 다시 연다.

- 교사 기기는 **최근에 연 코드 5개**를 기억한다. 2차시에 실수로 [수업 열기] 를 먼저 눌러도
  1차시 코드가 사라지지 않고, 칸에 자동으로 채워진다
- 코드가 둘 이상이면 `ZGPGXH · 어제` 같은 칩으로 골라 누를 수 있다
- 다른 기기에서 2차시를 열더라도 코드 6자리만 알면 그대로 이어진다
- 서버 보관은 **8일**이다. 그 뒤에는 같은 코드로 열어도 빈 수업으로 시작한다

**학생 각자의 기록은 코드와 상관없이 남는다.** 예상·이유·확인 방법·측정값·결론은
그 기기의 localStorage 에 있어서, 새 코드로 2차시를 열어도 자기 것은 그대로 되살아난다.
그래서 단계 6 되짚기 화면은 반 전체 자료가 없으면 **자기가 고른 예상과 적어 둔 방법**으로
대신 채우고, 그것마저 없으면 지난 코드로 열라고 안내한다.

## 가설 세우기 (단계 5-3)

"밝아서 그렇다"는 답을 알려 주지 않고, 과학자가 밟는 순서를 그대로 밟게 한다.

1. **예상을 고른다** — 더 밝은 / 더 큰 / 더 가까운 / 더 중요한 별이라서
2. **왜 그렇게 생각했는지** 한 줄로 적는다 (안 써도 넘어간다)
3. **확인할 방법**을 한 줄로 적는다 — 여기가 이 화면의 핵심이다

[가설 내기]를 누르면 교사 화면에 **우리 반이 말한 확인 방법**이 닉네임과 함께 줄줄이 쌓인다.
교사는 그중 "재 보면 된다"는 답을 짚어 다음 차시로 넘긴다.
학생이 스스로 말한 방법으로 다음 시간에 실제로 재기 때문에, 측정이 시키는 일이 아니라
자기가 세운 계획이 된다.

### 확인할 가설 고르기 (단계 5-4)

예상은 넷으로 갈리지만 **오늘 재서 확인할 수 있는 것은 하나뿐이다.**
그것을 학생과 함께 가려내는 화면이다.

- 별의 실제 **크기·거리**는 돌에 새긴 자국을 재는 것만으로는 알 수 없다
- **중요도**는 사람마다 다르게 정하는 것이라 재서 맞다 틀리다 할 수 없다
- **밝기**는 이미 숫자로 정해져 있어, 자국 지름과 견주면 바로 판정된다

그래서 "밝은 별일수록 크게 새겼을 것이다"를 **오늘의 가설로 못 박고** 1차시를 닫는다.
답을 알려 주는 것이 아니라 **검증 가능한 가설을 고르는 것**이며, 이 화면이 없으면
바로 뒤의 밝기 순서 맞히기와 영상이 뜬금없어진다(영상에도 밝기 이야기가 나온다).

반이 무엇을 골랐든 화면 위에는 낸 예상이 모두 그대로 표시되므로,
"밝기가 정답"이라고 못 박는 것이 아니라 "이것부터 확인해 보자"가 된다.

1차시 마지막 화면(5-7)에서는 이 가설과 자기가 적은 예상·이유·방법을 함께 보여 주고,
2차시 첫 화면(6)과 마지막 화면(10)에서 각각 되짚는다.

## 월하정인 (2차시 도입 · 단계 6)

측정으로 바로 들어가지 않고, 고천문학이 무엇인지 한 번 더 보여 주는 화면이다.

신윤복 「월하정인」의 달은 **아래쪽이 밝은 이상한 모양**인데, 그림에는 자정 무렵이라
적혀 있다. 초승달·그믐달 도식과 나란히 놓고 "자정에 이렇게 얇은 달이 떠 있을 수 있을까"를
묻는다. 학생이 고르고 이유를 적으면, 이 달을 월식으로 보고 기록과 대조해 날짜를 추정한
연구가 있다는 것을 알려 준다(다른 해석도 있다는 것을 함께 밝힌다).

핵심은 답이 아니라 **방법**이다 — 옛 기록을 오늘의 자료와 맞춰 보는 것.
바로 다음 화면부터 학생이 그 일을 직접 한다.

그림 파일은 저작권 확인 후 교사가 `public/assets/moon.jpg` 로 넣거나,
`?admin=1` → 교사 설정 → 수업 자료에서 경로를 지정한다. 없으면 안내만 뜨고 수업은 진행된다.

## 밝기 순서 맞히기 (1차시 끝 · 단계 5-5)

표를 그냥 읽기 전에 먼저 맞혀 보게 한다. 태양·보름달·금성·목성·시리우스·베가·데네브
일곱 개를 밝은 것부터 놓는다. 카드를 누르면 순서대로 놓이고, 놓은 카드를 누르면 빠진다.

[제출하기] 를 누르면 교사 화면에 **7 × 7 표**가 뜬다. 가로가 순위, 세로가 천체이고,
칸이 진할수록 그렇게 답한 학생이 많다. 실제 순위 자리에는 테두리를 둘러, **대각선이
밝게 켜지면 반이 잘 맞힌 것**이다. 어디서 갈렸는지가 한눈에 보인다(금성과 목성이 흔히 갈린다).

**등급 값은 그 자리에서 알려 주지 않는다.** 실제 등급은 다음 시간 단계 7-7 에서 밝힌다. 그 표에는 학생이 낸 순위가 "내 예상 3위" 처럼 함께 표시되고,
맞힌 것에는 ✓ 가 붙으며, 위의 7 × 7 집계표가 같은 화면에 다시 뜬다.

## 반 전체로 보기

혼자 잰 점 10개로는 흩어짐이 커서 경향이 흐릿하다.
그래서 참여한 학생들의 점을 한 그래프에 모을 수 있게 했다.

- **단계 9** — [우리 반 전체]를 누르면 친구들의 점이 옅게 깔리고,
  경향선을 **모든 점**으로 다시 구한다. 사람이 많아질수록 실수는 서로 상쇄되고
  진짜 경향만 남는다는 것을 눈으로 보게 하는 것이 목적이다.
  잰 것이 없는 기기(교사용)는 처음부터 반 전체가 켜진 채로 열린다.
- **단계 10** — 결론을 쓰고 [모두의 결론 보기]를 누르면 내 결론이 올라가고,
  친구들의 결론이 포스트잇처럼 본문 양옆에 흐른다(왼쪽은 위로, 오른쪽은 아래로).
  손을 올리면 멈춘다. 1400px 미만이거나 인쇄할 때는 흐르지 않고 그냥 늘어놓는다.

올라가는 것은 **[등급, 지름] 짝**과 **결론 한 문장**뿐이고, 산점도용 점은
누가 올렸는지 떼고 좌표만 모은다. 연결이 없으면 두 기능 모두 조용히 접히고
원래대로 내 결과만 보인다.

## 데모봇 — 수업 전에 혼자 돌려 보기

수업을 열면 교사 화면에 **[데모봇으로 시뮬레이션 하기]** 가 나온다.
가짜 학생 12명이 진짜 학생과 똑같이 붙어서, 교사가 화면을 넘길 때마다
그 단계에 맞는 일을 한다.

| 교사가 넘긴 단계 | 데모 학생이 하는 일 |
|---|---|
| 1 | 하나씩 들어온다 (약 0.4초 간격) |
| 5-3 | 1~7초에 걸쳐 예상을 고르고, **확인할 방법**을 한 줄씩 적어 낸다 |
| 5-5 | 1~9초에 걸쳐 밝기 순서를 낸다 (금성·목성이 갈리도록) |
| 8 | 별을 하나씩 재 나간다 — 속도가 사람마다 다르고, 끝까지 못 가는 학생도 나온다 |
| 10 | 1~9초에 걸쳐 결론을 쓴다 |

건너뛰어 들어가도 앞 단계에서 했어야 할 일을 몰아서 하므로 빈 화면이 되지 않는다.
사람마다 **치우침(±2.2px)과 흔들림(1.2~4.0px)** 이 달라, 반 전체로 모았을 때
경향이 또렷해지는 모습이 실제와 같이 나온다.

**[데모 멈추기]** 를 누르면 데모 학생이 남긴 것(명단·표·측정값·결론)을 서버에서
모두 지우고 빠진다. 그래서 리허설한 그 코드로 바로 수업해도 데모 자료가 섞이지 않는다.
이미 학생이 참여 중이면 시작 전에 한 번 묻는다.

교사 화면이 새로고침되어 정리 신호를 못 보냈더라도, 다음에 데모를 시작할 때
남아 있던 데모 자료를 먼저 걷어낸다(데모봇 토큰이 `demo-` 로 시작해 구분된다).

## 두 지도의 좌표 관계

`chart_orion.jpg`(각석본)와 `chart_color.jpg`(채색본)는 **같은 하늘을 담았지만 크롭이 다르다.**
채색본이 약 9% 작고 322px 위에 있다. 그래서 좌표를 그대로 쓸 수 없고,
설정의 `colorTransform` 으로 옮긴다.

```
각석본 → 채색본 :  배율 0.9073,  회전 −0.26°,  이동 (75, −322)
```

교사가 두 지도에 같은 별 6개를 표시해 준 대응으로 구했고 잔차는 2~16px다.
단계 4의 별 3개 확대와 밀어보기가 이 값을 쓴다.
채색본이 각석본 위쪽 355px 을 덮지 못하므로, 밀어보기는 겹치는 영역만 보여 준다.

## 무엇을 재는가 (측정 기준)

원본 유물을 확대해 보면 **밝은 별일수록 크고 깊게 파여 가운데가 비고 테두리만 진한 "고리"**로 보인다.
어두운 별은 작고 속이 찬 점에 가깝다. 따라서 측정 대상은 '가장 긴 지름'이 아니라
**바깥 테두리의 바깥쪽 끝에서 끝까지의 지름**이다.
앱은 이 기준을 코드로 그린 고리 도식과 함께 측정 화면·도움말·인쇄물에서 안내한다.

측정 뷰의 확대 배율은 자국이 화면에서 **최소 60px 이상**으로 보이도록 자동으로 잡는다
(자국 8~60px → 3~14배).

## 측정할 별 10개

교사가 각석본에서 직접 찍어 확정한 좌표다. 삼수(參宿)를 중심으로 등급이 고르게 퍼지도록 골랐다.

| 번호 | 별 | 전통 이름 | 등급 |
|---|---|---|---|
| 1 | 시리우스 | 천랑(天狼) | −1.5 |
| 2 | 리겔 | 삼수 일곱째 별(參宿七) | 0.1 |
| 3 | 베텔게우스 | 삼수 넷째 별(參宿四) | 0.4 |
| 4 | 알데바란 | 필수 다섯째 별(畢宿五) | 0.9 |
| 5 | 벨라트릭스 | 삼수 다섯째 별(參宿五) | 1.6 |
| 6 | 알니탁 | 삼수 첫째 별(參宿一) | 1.7 |
| 7 | 사이프 | 삼수 여섯째 별(參宿六) | 2.1 |
| 8 | 민타카 | 삼수 셋째 별(參宿三) | 2.2 |
| 9 | 하트샤 | 벌(伐) | 2.8 |
| 10 | 메이사 | 자수 첫째 별(觜宿一) | 3.4 |

확정된 별들의 자국을 실측하니 **등급과 지름의 상관이 −0.734** 로,
수업에서 보여주려는 규칙이 이 자료에서 실제로 성립한다.

알데바란은 확정별에서 15.8° 떨어져 있어 위치 확신이 가장 낮다.
어긋나 보이면 캘리브레이션에서 바로잡으면 된다.

## 인쇄

- **[나의 탐구 보고서 만들기]** — 학교·학년·반·번호·이름을 입력하면 탐구 질문 / 나의 예상 / 측정 결과 표 /
  그래프 / 내 결론 / 오늘의 정리 / 출처가 담긴 A4 보고서를 브라우저 인쇄로 뽑는다.
  PDF 저장은 인쇄 대화상자에서 고르면 된다.
- **[빈 기록표 인쇄]** — 태블릿이 없는 학급용. 측정 기준 도식 + 빈 기록표 + 빈 모눈 그래프.

## 폴더 구조

```
wrangler.jsonc             Cloudflare Worker 설정
worker/
  index.js                 라우팅 · 수업 코드 발급 · WebSocket 넘기기
  session-do.js            Durable Object — 참여자·투표·확인 방법·순위·단계·측정값·결론
  protocol.js              메시지 형식 · 수업 코드 만들기
public/                    Workers Assets 가 그대로 내보내는 화면
  index.html
  _headers                 캐시 설정
  config.json              교사 캘리브레이션 결과
  assets/
    style.css              화면 + 인쇄(@media print) 스타일
    js/constants.js        앱 문구 · 별 10개 데이터 · 확대 배율 정책
    js/guide.js            측정 기준 문구 + 고리 도식(SVG)
    js/config.js           교사 설정 로드·저장
    js/state.js            학생 진행 상태(localStorage)
    js/live.js             학급 연결 — 참여·닉네임·투표·확인 방법·순위·단계·측정값·결론
    js/demo.js             데모봇 — 버튼을 누를 때 읽는다
    js/skydata.js          밤하늘 레이어용 별 자국 542개
    js/mapview.js          지도+핀 / 별 확대창 / 세 층 밀어보기
    js/measure.js          확대 측정 뷰(두 손잡이)
    js/chart.js            점 그래프(산점도) SVG — 내 점 / 반 전체 점
    js/steps.js            10단계(두 차시) 진행 controller
    js/report.js           보고서·기록표 인쇄, CSV
    js/admin.js            교사 캘리브레이션 — ?admin=1 일 때만 읽는다
    js/detect.js           각석본에서 별 자국을 찾는 도구. skydata 를 다시 구울 때만 쓰며
                           화면에서는 읽지 않는다(필요하면 콘솔에서 직접 불러온다)
    chart_orion.webp       각석본 2280 × 2480 (측정용)
    chart_color.webp       채색본 2280 × 2480
    chart_full.webp        전체 지도 540 × 622
    chart_stone.jpg        각석 실물 사진 1600 × 1048
    bill.webp              만원권 뒷면 사진
    2-2 2-3 2-5.webp       단계 3 이야기 카드 삽화(교사 제작)
    grade.jpg star.webp hipa.webp  단계 7 등급 설명 그림
    moon.webp              단계 6 「월하정인」 그림
    bgm.mp3                배경음악(교사 제작) — 기본 꺼짐
```

## 교사 준비 순서

주소 뒤에 **`?admin=1`** 을 붙이면 캘리브레이션 모드로 들어간다.

1. **① 측정 핀** — [별 찍기 시작]을 누르면 별 이름이 하나씩 뜨고, 지도에서 그 별을
   **한 번 누르면** 그 자리로 확정되고 다음 별로 넘어간다. 옆 확대 창으로 자국 한가운데인지
   확인하고, 어긋나면 창 안을 끌어 원본 픽셀 단위로 미세 조정한다.
   [채색본으로 보기]로 배경을 바꿔 한자 이름을 확인할 수 있다.
   핀은 옮기는 즉시 저장되므로 새로고침해도 날아가지 않는다.
2. **② 단계 4 확대 영역** — 전체 지도에서 삼수 자리에 사각형을 맞춘다.
   ⚠ 기본값은 눈으로 확인하지 않은 어림값이므로 **반드시 확인이 필요하다.**
   맞춘 뒤 "이 영역을 눈으로 확인함"을 켠다.
3. **③ 단계 5 별 3개** — 크기 차이가 잘 드러나는 밝은·중간·어두운 별을 고른다.
4. **④ 수업 자료** — 단계 2·5·6 의 유튜브 주소와 단계 6 의 「월하정인」 그림 경로를 넣는다.
   비워 두면 그 화면에 "설정되지 않았습니다" 안내가 뜨고, 수업은 그대로 진행된다.
5. **[서버에 저장]** — 개발 서버로 열었다면 `config.json` 이 저장소에 바로 써진다.
   그 파일을 커밋해 배포하면 학생 태블릿이 모두 그 설정을 쓴다.
   (다른 환경이면 [config.json 내보내기] 로 내려받아 저장소 최상위에 둔다)

설정 우선순위는 **`config.json` → localStorage → 기본값**.
"config.json 보다 이 기기 설정을 우선 사용"을 켜면 그 기기에서는 localStorage가 이긴다.

## 배포

**Cloudflare Workers** 로 배포한다. 학급 실시간 투표가 Durable Object 를 쓰므로
Pages 가 아니라 Worker 여야 한다.

```bash
npm install
npx wrangler login      # 처음 한 번만
npm run deploy
```

주소는 `https://josunstellar.<계정>.workers.dev` 형태로 나온다.
`public/` 안의 화면·이미지는 Workers Assets 가 그대로 내보내므로 빌드 단계가 없다.

- 교사용 — 주소 뒤에 `?admin=1`
- 학생 — 첫 화면에서 [코드로 참여하기]

캘리브레이션을 고쳤으면 `public/config.json` 을 커밋하고 다시 배포한다.

### 학급 기능을 빼고 정적으로만 쓰려면

`public/` 폴더만 아무 정적 호스팅에 올려도 된다.
Cloudflare Pages 는 build output directory 를 `public`, GitHub Pages 는 `/public` 으로 둔다.
참여 칸은 스스로 접히고 나머지 10단계는 그대로 동작한다.
`_headers` 가 캐시를 잡아 준다 — 지도 이미지는 7일, 코드(HTML·JS·CSS)는 매번 확인.
코드에 max-age 를 주면 안 된다. index.html 만 새것이고 JS 가 낡으면 화면과 코드가
어긋난 채로 돌아가, 배포 직후 그 시간 동안 학생 기기에서 앱이 깨질 수 있다.
이미지를 교체했는데 옛 그림이 보이면 파일 이름을 바꿔 올리면 바로 반영된다.

## 개발용 서버

```bash
npm run dev        # wrangler dev — 학급 기능까지 그대로 돌아간다
```

`node tools-serve.js 8123` 은 Worker 없이 화면만 볼 때 쓰는 간이 서버다.
캘리브레이션의 [서버에 저장] 은 이 간이 서버에서만 동작한다
(`wrangler dev` 로 띄웠을 때는 [config.json 내보내기] 를 쓴다).

## 주의

- 겉보기 등급은 **근사값(≈)** 이며 베텔게우스는 변광성이라 대표값이다.
- 이 지도는 600년 전 유물이라 현대 관측값과 오차가 있다. 그래도 등급과 자국 크기의
  관계는 뚜렷해서 수업에서 보여주려는 규칙은 충분히 드러난다.
